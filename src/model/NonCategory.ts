export type NonCategoryProps = {
  _id: Record<string, string>;
  total: number;
  user: string;
  userId: string;
  finClass: string;
  corpName: string;
  lastDate: string;
  category: string;
  changeCategory: string;
  changeCategoryName: string;
};
